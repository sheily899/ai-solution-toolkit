# AskData / NL2SQL

## 定位

面向不会写 SQL 的业务人员，探索通过自然语言完成受控数据库查询的 PoC。

## 当前链路

路由 → Schema 混合检索 → Schema 约束 → SQL 生成 → 安全校验 → MCP/数据库执行 → 表格与自然语言结果。

## 当前评测

已进行小规模 BIRD 数据试跑。现有结果只代表当前样本和版本，后续以可复现评测记录为准。

## 下一步

- 补齐评测 Trace；
- 区分检索、关联、生成、安全和执行失败；
- 建立正常、歧义、越权和异常恢复用例；
- 用 DeepWiki/GitDiagram 核验和展示项目结构；
- 用 Visual Explainer 生成评测复盘页。
